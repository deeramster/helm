---
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets-{{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/instance: "{{ .Release.Name }}"
    app.kubernetes.io/version: "{{ .Chart.AppVersion }}"
    app.kubernetes.io/managed-by: "{{ .Release.Service }}"
    helm.sh/chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: sausage-store
type: Opaque
stringData:
  spring-datasource-url: {{ .Values.secrets.springDatasourceUrl | quote }}
  postgres-user: {{ .Values.secrets.postgresUser | quote }}
  postgres-password: {{ .Values.secrets.postgresPassword | quote }}
  mongo-user: {{ .Values.secrets.mongoUser | quote }}
  mongo-password: {{ .Values.secrets.mongoPassword | quote }}
  mongo-host: {{ .Values.secrets.mongoHost | quote }}
  mongo-dbname: {{ .Values.secrets.mongoDbName | quote }}